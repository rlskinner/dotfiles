    TYPE = { critical, information, question, warning }
    StandardButton QMessageBox::TYPE(QWidget *parent, const QString &window-title, const QString &text, StandardButtons buttons = Ok, StandardButton defaultButton = NoButton);
